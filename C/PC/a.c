#include <stdio.h>
int main(){


/*
	// 1. 
	double d,h,result,pi;
	pi=3.14;
	printf("请输入圆柱体的底面半径:\n");
	scanf("%lf%lf",&d);
	printf("请输入圆柱体的高:\n");
	scanf("%lf%lf",&h);
	result = d*d*pi*h;
	printf("圆柱体的体积为=%.1f\n",result);

	// 2.
	float score;
	scanf("%f",&score);
	if(score >= 0 && score < 60){
		printf("你的成绩E"); 
	}else if(score < 70){
		printf("你的成绩D"); 
	}else if(score < 80){
		printf("你的成绩C"); 
	}else if(score < 90){
		printf("你的成绩B"); 
	}else if(score < 100){
		printf("你的成绩A"); 
	}else printf("你的成绩有问题"); 
*/

/*
int b[ ][3]={
	{1},
	{3,2},
	{4,5,6},
	{0}
};
	int i,n=0;
	for(i=1; i <= 5; i+=2){
		printf("执行一次\n");
	}

printf("\nb[][3]=%d",b[2][2]);

	printf("\n浮点数占：",sizeof(float));
printf("%f",2.000000000-1.0);
	return 0;
} 

/*
for(i=2; i=0; ; )
	printf("%d",i--);

*/


int i,a;
i = (a=4,2*6);
printf("%d",i);


	return 0;
}


/*
	int in[Len],i,n;
	srand(time(NULL));

	//随机产生数
	for(i=0;i<Len;i++)
		a[i] = rand()% (100-50+1) + 50;

	//输出结果
	for(i=0;i<Len;i++)
		printf("%d|",a[i]);	
	

	int a,b;
	a = 3;
	b = 4;
	a += b;
	b = a -b;
	a -= b;
	printf("a=%d,b=%d",a,b);


*/